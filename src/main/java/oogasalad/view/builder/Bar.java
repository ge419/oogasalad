package oogasalad.view.builder;

import java.util.ResourceBundle;

public interface Bar {
  void addItems(ResourceBundle resourceFunctionHolder);
  void refreshItems(ResourceBundle newResourceFunctionHolder);
}
